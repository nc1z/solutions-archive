{
  "name": "solutions-archive",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "schedule": "npx tsc && node dist/asyncScheduler/example.js",
    "debounce": "npx tsc && node dist/debounce/example.js",
    "promiseAll": "npx tsc && node dist/promiseAll/example.js",
    "throttle": "npx tsc && node dist/throttle/example.js",
    "promiseAny": "npx tsc && node dist/promiseAny/example.js",
    "lru": "npx tsc && node dist/LRUCache/example.js",
    "build": "npx tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^22.7.5",
    "typescript": "^5.6.3"
  }
}
